package apicore;


import org.dom4j.Document;
import org.dom4j.Element;

import java.util.List;


public class RestHttpServer {

 public void test(){
     /**Document document = null;

     Document dom = new SAXReader().read(url);

     List<Element> elementList = dom.getRootElement().elements("name");
     // scan through the job list and print its status
     for( Element job : elementList) {
         System.out.println(job.elementText("name")+ job.elementText("color"));
     }
      **/
 }
}
